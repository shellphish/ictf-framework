from distutils.core import setup

setup(
    name='scoring_ictf',
    version='0.0.1',
    description='This is a python module that implements the scoring for the UCSB iCTF',
    packages=['scoring_ictf'],
    install_requires=[
    ],
)
